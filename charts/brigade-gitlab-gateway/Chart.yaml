apiVersion: v1
description: Brigade GitLab Gateway provides events from GitLab to the Brigade pipeline
name: brigade-gitlab-gateway
version: 0.10.0
# Note that we use appVersion to get images, so make sure this is correct.
appVersion: v0.10.0
